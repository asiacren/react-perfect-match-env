import React from 'react';
import {style} from './index.scss';

const Test = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Test.propTypes = {

};

export default Test;
